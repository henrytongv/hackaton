# Server Backend java spring boot
Backend service that Generates a new Hedera Account using the Hedera java SDK

Can be deployed anywhere java runs. For this hackaton it was deployed as a Google Cloud Run service

# How to deploy to google cloud
gcloud run deploy --source .